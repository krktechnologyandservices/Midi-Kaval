<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches RNFS.CachesDirectoryPath, where openAttachmentFile.ts writes
         downloaded attachments before handing them to the native viewer. -->
    <cache-path name="attachments" path="." />
</paths>
