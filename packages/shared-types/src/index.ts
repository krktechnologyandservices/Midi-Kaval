export enum AppRole {
  Director = 'Director',
  Coordinator = 'Coordinator',
  SocialWorker = 'SocialWorker',
  CaseWorker = 'CaseWorker',
}

export enum SyncState {
  Local = 'local',
  Pending = 'pending',
  Synced = 'synced',
  Error = 'error',
}
